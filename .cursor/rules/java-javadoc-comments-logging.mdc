---
description: Javadoc、中文注释与 SLF4J 日志规范
alwaysApply: true
---

# 注释规范

- **对外接口**（public API、Controller 暴露的契约、跨模块 interface 等）必须写 **Javadoc**（`/** ... */`），说明用途、参数、返回值、异常（如有）。
- 实现类中在**必要且重要**的逻辑处写注释；**注释正文一律使用中文**。
- 注释中涉及的 **类名、方法名、变量名、框架 API 名** 等本身为英文的可保持英文，勿生硬翻译。

# 日志（SLF4J）

- 统一使用 **SLF4J**（如 `LoggerFactory.getLogger(...)`），勿随意混用 `System.out` 或裸 `printStackTrace`。
- **与外部交互的边界**（HTTP 入站、RPC、MQ 消费/生产等）：**进入**、**正常返回**、**异常** 路径都要有日志；重点 **业务步骤** 在关键节点打日志。
- **日志级别**：
  - **INFO**：正常业务流程、可审计的关键信息。
  - **WARN**：不影响主流程继续，但属于**非正常业务分支**或需关注的状态。
  - **ERROR**：异常、失败、需排查的错误（通常伴随异常对象或明确错误原因）。

注意避免敏感数据（密码、token、完整证件号等）写入日志。
